# Compose Services

- `checks`: executes validation commands inside the build environment.
- `portfolio`: runs the production runtime image.

Compose is the canonical entry point for verification and local execution.
