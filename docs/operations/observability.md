# Observability

- Structured logs for startup and route failures.
- Health endpoint for service liveness.
- Job snapshots expose progress and terminal output.

The system avoids hidden retries and surfaces errors explicitly.
