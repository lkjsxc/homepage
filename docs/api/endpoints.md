# Endpoints

## `POST /api/v1/jobs`
Creates a new asynchronous job.

## `GET /api/v1/jobs`
Lists known jobs in reverse creation order.

## `GET /api/v1/jobs/{id}`
Returns the latest snapshot for one job.

## `GET /api/v1/health`
Returns process health and version metadata.
